# Keep this file free of real secrets. Configure the actual value only on the server.
GROQ_API_KEY=
# Use a model available to your Anthropic account.
GROQ_MODEL=qwen/qwen3.6-27b
# Comma-separated browser origins permitted to call this API.
CORS_ORIGIN=http://localhost:5000,http://127.0.0.1:5000
